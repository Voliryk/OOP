#include <iostream>
#include <cmath>

int main() {
    double a, b, h;
    std::cin >> a >> b >> h;

    std::cout << "x\tY\n"; 
    std::cout << "---------\n";

    for (double x = a; x <= b; x += h) {
        double y = tan(x) + sqrt(x) + 2;
        std::cout << x << "\t" << y << "\n";
    }
}
